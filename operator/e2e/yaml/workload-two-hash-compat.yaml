# Workload for the canonical/legacy hash compatibility upgrade test.
---
apiVersion: grove.io/v1alpha1
kind: PodCliqueSet
metadata:
  name: two-hash-compat
  labels:
    app: two-hash-compat
spec:
  replicas: 1
  template:
    cliques:
      - name: z-standalone
        labels:
          kai.scheduler/queue: test
        spec:
          roleName: standalone
          replicas: 1
          minAvailable: 1
          podSpec:
            terminationGracePeriodSeconds: 5
            containers:
              - name: sidecar
                image: registry:5001/busybox:latest
                command: ["sleep", "infinity"]
                volumeMounts:
                  - name: shared
                    mountPath: /shared
                  - name: cache
                    mountPath: /cache
                resources:
                  requests:
                    memory: 16Mi
              - name: main
                image: registry:5001/busybox:latest
                command: ["sleep", "infinity"]
                volumeMounts:
                  - name: shared
                    mountPath: /shared
                  - name: cache
                    mountPath: /cache
                resources:
                  requests:
                    memory: 16Mi
            volumes:
              - name: shared
                emptyDir: {}
              - name: cache
                emptyDir: {}
      - name: a-grouped
        labels:
          kai.scheduler/queue: test
        spec:
          roleName: grouped
          replicas: 1
          minAvailable: 1
          podSpec:
            terminationGracePeriodSeconds: 5
            containers:
              - name: sidecar
                image: registry:5001/busybox:latest
                command: ["sleep", "infinity"]
                volumeMounts:
                  - name: shared
                    mountPath: /shared
                  - name: cache
                    mountPath: /cache
                resources:
                  requests:
                    memory: 16Mi
              - name: main
                image: registry:5001/busybox:latest
                command: ["sleep", "infinity"]
                volumeMounts:
                  - name: shared
                    mountPath: /shared
                  - name: cache
                    mountPath: /cache
                resources:
                  requests:
                    memory: 16Mi
            volumes:
              - name: shared
                emptyDir: {}
              - name: cache
                emptyDir: {}
    podCliqueScalingGroups:
      - name: sg
        replicas: 1
        minAvailable: 1
        cliqueNames:
          - a-grouped
