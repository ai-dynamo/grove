# E2E test setup preset — functional tests, KWOK for test pods, no Pyroscope.

cluster:
  create: true
  worker_nodes: 2
  worker_memory: "500m"

kwok:
  nodes: 30
  node_cpu: "4"
  node_memory: "8Gi"

scheduler:
  kai:
    enabled: true

grove:
  enabled: true
  profiling: false
