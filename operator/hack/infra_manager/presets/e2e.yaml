# E2E test setup preset — functional tests, no KWOK, no Pyroscope.

cluster:
  create: true
  config:
    worker_nodes: 30
    worker_memory: "150m"

kai:
  enabled: true

grove:
  enabled: true
  profiling: false
